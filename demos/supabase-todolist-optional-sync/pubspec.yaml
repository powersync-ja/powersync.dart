name: powersync_flutter_supabase_todolist_optional_sync_demo
description: PowerSync Flutter Supabase Todolist Optional Sync Demo
publish_to: "none"
resolution: workspace
version: 1.0.1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  powersync: ^2.0.0
  sqlite3: ^3.2.0
  path_provider: ^2.1.1
  supabase_flutter: ^2.0.1
  path: ^1.8.3
  logging: ^1.2.0
  camera: ^0.12.0
  image: ^4.1.3
  universal_io: ^2.2.2
  sqlite_async: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
