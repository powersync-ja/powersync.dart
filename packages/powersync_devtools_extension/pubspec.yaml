name: powersync_devtools_extension
description: "DevTools extension for PowerSync."
publish_to: 'none'
resolution: workspace

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  devtools_extensions: ^0.5.0
  devtools_app_shared: ^0.5.0
  riverpod: ^3.2.1
  flutter_riverpod: ^3.3.1
  sqlite_async: ^0.14.0
  web: ^1.1.1
  vm_service: ^15.1.0
  sqlite3: ^3.3.1

  powersync:
  hooks_riverpod: ^3.3.1
  flutter_hooks: ^0.21.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
