# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: PowersyncFlutterLibsBindings
description: |
  Bindings for `src/powersync_flutter_libs.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/powersync_flutter_libs_bindings_generated.dart'
headers:
  entry-points:
    - 'src/powersync_flutter_libs.h'
  include-directives:
    - 'src/powersync_flutter_libs.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
